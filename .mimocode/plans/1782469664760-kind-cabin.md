# Plan: Perbaiki Task Pages — Lebih Advanced & Teks Jelas

## Masalah

1. **Kanban Board** menggunakan tema terang (`bg-white`, `bg-purple-100`, `bg-blue-100`) — clash total dengan dashboard gelap (`bg-[#16162a]`, `bg-[#0c0c1d]`)
2. **Teks terlalu pudar** — banyak menggunakan `text-white/40`, `text-white/30`, `text-white/20` yang sulit dibaca
3. **Task detail pages** menggunakan `glass` dan `border-border/50` yang tidak konsisten dengan dark theme
4. **Badge status** pada task detail menggunakan warna terang (`text-green-600`, `text-blue-600`) yang kurang kontras di dark background

## Files yang akan diubah

### 1. `components/owtask/task/kanban-board.tsx` — OVERHAUL

Ubah dari tema terang ke tema gelap yang konsisten:

- Column backgrounds: `bg-purple-50/80` → `bg-white/[0.03]` dengan border `border-white/[0.06]`
- Card backgrounds: `bg-white` → `bg-[#1e1e3a]` dengan border `border-white/[0.08]`
- Tag colors: `bg-purple-100` → `bg-purple-500/15` dengan `text-purple-300`
- Progress bar bg: `bg-gray-100` → `bg-white/[0.06]`
- Text colors: `text-muted-foreground` → `text-white/50`
- "No tasks yet" text: `text-muted-foreground/60` → `text-white/20`
- "Add task" button: border `border-gray-200` → `border-white/[0.08]`
- Column header badge: gunakan style yang sesuai dark theme
- Avatar fallback: border `border-white` → `border-[#1e1e3a]`
- Heading text: pastikan `text-white` untuk visibility

### 2. `app/owtask/dashboard/leader/tasks/page.tsx` — Text & Card Fixes

- Subtitle: `text-white/40` → `text-white/60`
- Card title: `text-white/80` → `text-white/90`
- Card description: `text-white/40` → `text-white/55`
- Card meta (avatar labels, due date): `text-white/30` → `text-white/45`
- Empty state icon: `text-white/20` → `text-white/25`
- Empty state text: `text-white/40` → `text-white/55`

### 3. `app/owtask/dashboard/worker/tasks/page.tsx` — Text & Card Fixes

- Same text opacity fixes as leader tasks page
- Card title: `text-white/80` → `text-white/90`
- Card description: `text-white/40` → `text-white/55`
- Meta text: `text-white/30` → `text-white/45`

### 4. `app/owtask/dashboard/leader/tasks/[id]/page.tsx` — Dark Theme Consistency

- Title: tambahkan `text-white`
- Status badge: `text-green-600` → `text-emerald-400`, `text-blue-600` → `text-blue-400`, dll
- Card backgrounds: `glass` → `bg-[#1e1e3a]` dengan `border-white/[0.08]`
- CardHeader titles: tambahkan `text-white`
- Description/body text: `text-muted-foreground` → `text-white/60`
- Separator: sesuaikan warna
- Detail labels: `text-muted-foreground` → `text-white/50`
- Detail values: pastikan `text-white/80`
- Submissions: background & text yang sesuai dark theme
- Empty state: `text-muted-foreground` → `text-white/40`

### 5. `app/owtask/dashboard/worker/tasks/[id]/page.tsx` — Dark Theme Consistency

- Sama dengan leader task detail
- Title: tambahkan `text-white`
- Status badge colors: gunakan warna terang untuk dark bg
- Card backgrounds & text: sesuaikan dengan dark theme
- Submission form: textarea placeholder & styles yang sesuai

## Verification

1. Jalankan `pnpm lint` setelah semua perubahan
2. Jalankan `pnpm build` untuk memastikan tidak ada error
3. Visual check: buka halaman Tasks (leader & worker) — pastikan teks terbaca jelas, kanban board match dengan dashboard theme
