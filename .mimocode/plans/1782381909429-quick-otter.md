# Plan: Major UI Redesign — Icons, Calendar, Payment, Visual Polish

## Goal

Overhaul all icons (no emoji), add colorful Kanban cards with progress bars, weekly calendar view, payment page, and more interactive landing page. Inspired by 6 reference images.

---

## 1. Global Icon Cleanup — Replace ALL emoji with lucide-react

**Files:** Every file that uses emoji characters

- `components/owtask/landing/hero.tsx` lines 104-106: Replace 📋✅👥 with `<ClipboardCheck>`, `<CheckCircle>`, `<Users>` from lucide-react
- `app/owtask/dashboard/leader/page.tsx`: Verify no emoji in stat cards
- `app/owtask/dashboard/worker/page.tsx`: Verify no emoji in stat cards
- `components/owtask/chat/chat-message.tsx`: Already uses lucide icons, verify

---

## 2. Glassmorphism Icon Component (Image 3 style)

**New file:** `components/owtask/shared/glass-icon.tsx`

Create a reusable icon component that matches Image 3:

- Two overlapping rounded-square frames (85x100px proportion)
- Back frame: solid gradient fill
- Front frame: glass effect with backdrop-blur, semi-transparent
- Drop shadow on back frame
- Lucide icon centered inside

```tsx
// Gradient presets: emerald, blue, purple, amber, rose
// Size presets: sm (40px), md (56px), lg (72px)
```

---

## 3. Colorful Kanban Board (Image 1 + Download Kanban style)

**File:** `components/owtask/task/kanban-board.tsx`

Redesign task cards to match reference:

- **Column headers**: Colored dots with pastel backgrounds
- **Task cards**: Each column gets a unique pastel background tint:
  - To Do: soft purple/lavender (#F3F0FF)
  - In Progress: soft blue (#EBF5FF)
  - Done: soft green (#ECFDF5)
  - Overdue: soft rose (#FFF1F2)
- **Progress bar**: Gradient-filled progress indicator on each card
- **Tags**: Hashtag-style colored tags (#website, #mobile, #client)
- **Member avatars**: Colored circle avatars with initials
- **Card actions**: Comment count, attachment count with icons

---

## 4. Weekly Calendar View (Image 2 style)

**File:** `app/owtask/dashboard/calendar/page.tsx`

Complete rewrite to weekly view:

- **Header**: "All Scheduled", "Events", "Meetings", "Task Reminders" tabs
- **Sub-header**: Month + Today button, Day/Week/Month toggle, date range
- **Grid**: 7 columns (Mon-Sun), time slots from 8AM to 5PM
- **Events**: Colored blocks positioned by time, with:
  - Green: meetings
  - Purple: deadlines
  - Blue: tasks
  - Pink: personal
- **Event cards**: Title, time range, category tag
- **+ New button**: Opens event creation dialog

Keep the monthly view accessible via "Month" toggle.

---

## 5. Payment Page

**New file:** `app/owtask/dashboard/payment/page.tsx`
**New file:** `components/owtask/payment/payment-form.tsx`

Glassmorphism payment form (Image payment style):

- **Card preview**: Floating credit card visualization with gradient
- **Form fields**: Cardholder name, card number, expiry month/year, CVV
- **Payment methods**: Credit card, PayPal, Google Pay, Apple Pay icons
- **Amount display**: Total with currency
- **Pay button**: Gradient with loading state
- **Steps**: Main page → Shipping details → Payment method (stepper)

Add "Payment" to sidebar navigation.

---

## 6. Sidebar Redesign

**Files:** `components/owtask/sidebar/app-sidebar.tsx`, `sidebar-nav.tsx`

Polish to match Download 1 style:

- Active state: White background with subtle shadow, not gradient
- Inactive: Transparent with muted text
- Icon containers: Rounded-lg with subtle bg on hover
- Section labels: "MAIN", "RECORDS" groupings
- Better spacing and typography

---

## 7. Landing Page Enhancements

**Files:** `components/owtask/landing/hero.tsx`, `features.tsx`

- Replace all emoji in dashboard preview with lucide-react icons
- Add more interactive elements: hover effects on feature cards
- Better stat cards with gradient icons (using glass-icon component)
- Add "How it works" section with step-by-step illustrations
- Testimonials section with avatar + quote cards

---

## 8. Dashboard Stats Enhancement

**Files:** Leader + Worker dashboard pages

- Add gradient progress bars (not just numbers)
- Efficiency circle charts (like Image 1 right panel)
- Recent activity timeline with avatars
- Colorful bar charts matching the pastel theme

---

## Execution Order

1. Create glass-icon component
2. Fix all emoji → lucide icons across all files
3. Redesign Kanban board with colorful cards + progress
4. Rewrite calendar to weekly view
5. Create payment page + form
6. Polish sidebar
7. Enhance landing page
8. Enhance dashboards
9. Build + lint verification

## Verification

- `pnpm build` — no errors
- `pnpm lint` — no warnings
- Navigate all routes visually
